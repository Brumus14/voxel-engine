When flying just above ground moves faster than should do
Stop polling chunks and use queues instead
Skip trying to render empty chunks maybe?
Upscale noise so have to sample less
When world load chunk add to queue and load x per update to avoid fps spike
Player manage chunks causes lagging
Anti-aliasing
Framerate drops when loading new chunks
Falling fast can clip into block
Better textures
Update task priorities when moving
Render distance 0 edit terrain chunk and mesh always visible
Improve collision for fast moving
Neighbor loading stuff should be reviewed
Closing window doesn't clean up memory properly
*When two workers simultaneously generating mesh, both modify vertices and indices
With large chunk size loading new chunks still lags
Chunking is bottleneck not rendering
Support block transparency, e.g. leaves
Improve world generation, and add trees
Save chunk modifications when reloading a chunk
Save files
Use const void *
Order structs for padding and caching
Moving fast loads tons of chunks that take ages to unload
Maybe stop polling for chunk changes like MESH_NEEDED
Reduce looping in main, such as loading chunks in player
Add get functions to renderer
Move keyboard bindings from main
Maybe optimise thread pool queue for SPMC
Improve meshing like greedy meshing
Better lighting
Blocks orient correctly when placed
Maybe when modifying chunk don't re-mesh entire chunk just modified part
When moving fast lots of chunks are scheduled even if they're now out of render distance
Maybe rework chunk iteration as lots of argument parsing, maybe just access internal hash map
Maybe rework worker as lots of argument parsing
Chunk occlusion culling
I think rendering is like blocked especially noticable when initially generating lots of chunks
Pass by reference more
